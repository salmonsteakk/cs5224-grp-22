# Optional: Hugging Face token for AI chat and dashboard coach (chat completions API).
# Create at https://huggingface.co/settings/tokens
# The server reads HF_TOKEN first, then HF_API_KEY if HF_TOKEN is unset.
HF_TOKEN=
HF_MODEL=meta-llama/Llama-3.1-8B-Instruct:novita
